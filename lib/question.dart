class Question {
  String questionText;
  String questionImg;
  bool questionAnswer;

  Question({ required String q,  required String i,  required bool a}) :
    questionText = q,
    questionImg = i,
    questionAnswer = a;
  
}
